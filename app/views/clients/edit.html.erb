  <div class="container">
    <%= simple_form_for @client, url: client_path(@client) do |f| %>
      <%= f.input :name, label:"Nom" %>
      <%= f.input :photo, as: :file, label:"Photo de Profil" %>
      <%= f.submit "Modifier le profil client", class:"btn-primary" %>
    <% end %>
  </div>