<h1>Activités#edit</h1>
<div class="container">
  <div class="row flex-center">
    <div class="col-12 col-m-6 col-xl-4 my-4">
      <%= simple_form_for @activity, url: activity_path(@activity) do |f| %>
        <%= f.input :title, label:"Titre"%>
        <%= f.input :description, as: :rich_text_area, label:"Contenu" %>
        <%= f.input :photo, as: :file, label:"Photo" %>
        <%= f.submit "Modifier l'activité", class:"btn-primary" %>
      <% end %>
    </div>
  </div>
</div>